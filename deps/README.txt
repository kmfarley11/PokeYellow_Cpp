If deps are not found installed on the system already then the project will download sdl2 sdl2_image and gtest to here.
The project will download lib / src, extract, and copy necessary dlls to ../bin
